{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run current Julia file",
            "type": "julia",
            "request": "launch",
            "program": "${file}",    
            "cwd": "${workspaceFolder}", 
            "stopOnEntry": false
        }
    ]
}
